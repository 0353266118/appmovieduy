<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Nền trắng -->
    <solid android:color="@color/movie_backgroundColor_light" />
    <!-- Viền xám nhạt -->
    <stroke android:width="1dp" android:color="@color/movi_borderColor"/>
    <!-- Bo góc -->
    <corners android:radius="16dp" />
</shape>